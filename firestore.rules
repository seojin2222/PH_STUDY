rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // 로그인(Firebase Auth)한 직원만 모든 컬렉션을 읽고 쓸 수 있음.
    match /{collection}/{docId} {
      allow read, write: if request.auth != null;
    }
  }
}
